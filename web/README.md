# web — frontend (Next.js)

**Próxima fatia.** Aqui virá o app Next.js + React + TypeScript com o mapa semafórico do IVM e o
drill-down de município (componentes do design system: `Mapa`, `Semaforo`, `SerieTemporal`,
`Comparador`), consumindo a API pública `/v1/*`.

Pendências conhecidas (a especificar antes de codar): design system (tokens, componentes,
acessibilidade) e o fluxo de autenticação do cidadão (OIDC → JWT em cookie HttpOnly).
