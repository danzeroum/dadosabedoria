/** @type {import('next').NextConfig} */
const nextConfig = {
  // Imagem Docker enxuta (server.js + .next/standalone).
  output: "standalone",
  reactStrictMode: true,
};

export default nextConfig;
